import { ArrowRight, Home, Settings, UserPlus, Upload, Users } from "lucide-react";

export const NavigationIcons = {
  home: Home,
  dashboard: ArrowRight,
  settings: Settings,
  invite: Users,
  join: UserPlus,
  upload: Upload,
};
